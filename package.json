{
  "name": "homesphere-ai",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "description": "HomeSphere AI Monorepo Workspace",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "dependencies": {
    "cloudinary": "^2.10.0"
  }
}
